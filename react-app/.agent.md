# .agent.md — CopilotCLI などの CLI 系サブエージェント向け補助入口

このファイルは **CopilotCLI などの CLI 系サブエージェント** 向けの補助入口ファイルです。

## このリポジトリについて

本ディレクトリ（`react-app/`）は **SSOT 完成物プロファイル** です。アプリケーションコードは含みません。
target repo へそのままコピーして展開するために設計された、AI エージェント向けルールと入口ファイルの完成物セットです。

## 参照すべきファイル

エージェントは以下の順で参照してください。

1. `.github/copilot-instructions.md` — Copilot 共通入口（全エージェント共通の規範ルール）
2. `.github/copilot/00-index.md` — 文書索引（参照順）
3. `.github/copilot/10-requirements.md` — 管理対象・非対象・受入条件
4. `.github/copilot/20-architecture.md` — ディレクトリ構造・distribution_root・展開イメージ

## このファイルの責務

- CLI 系サブエージェントの補助入口として機能する
- 技術仕様・設計判断・実装ルールは `.github/copilot/00-index.md` を起点とする参照順に従う

## 出力言語

すべて日本語で出力する（レビューコメント・提案・要約・理由説明を含む）。

## 禁止事項

- SSOT に定義されていない新ルールをレビューや実装内で創作しない
- 指示されていないスコープ拡張・機能追加・最適化を行わない
- アプリケーションコードをこのリポジトリに生成しない
